{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main AI HUD overlay window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "global-shortcut:default"
  ]
}
